import { StyleSheet, Dimensions } from "react-native";

export default StyleSheet.create({
  container: {
    marginTop: 20,
    marginBottom: 20,
  },
  centeredText: {
    textAlign: "center",
  },
});
