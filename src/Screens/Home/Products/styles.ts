import { StyleSheet } from "react-native";

export default StyleSheet.create({
  heading: {
    textAlign: "center",
    marginBottom: 20,
  },
  productlist: {
    backgroundColor: undefined,
  },
});
